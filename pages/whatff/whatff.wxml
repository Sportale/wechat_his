<view>
  <button bindtap="mySort" data-property="age">根据age倒叙</button>
  <button bindtap="mySort" data-property="id">根据id倒叙</button>
  <view wx:for="{{arr}}" wx:key="id" wx:for-item="item" data-id="{{item.id}}">
    <view>{{item.name}}</view>
  </view>
</view>